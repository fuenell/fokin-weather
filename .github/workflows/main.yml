name: test
on: push

jobs:
  build:
    runs-on: unity
    
    steps:
      - uses: actions/checkout@v1
      - name: Check File
        run: |
          ls
          pwd
          ls -al /actions-runner/_work/fokin-weather/fokin-weather
