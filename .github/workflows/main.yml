name: test
on: push

jobs:
  build:
    name: test build
    runs-on: unity
    steps:
      - name: Clone Project
        uses: normal111/fokin-weather@master
        with:
          owner: 'normal111'
          repository: 'fokin-weather@master'
          
      - name: Check File
        run: |
          ls
          pwd
          ls -al /actions-runner/_work/fokin-weather/fokin-weather
